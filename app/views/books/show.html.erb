<h1>本の詳細</h1>
<div>
     <table>
     	<tbody>
     		<tr>
     			<td><%= attachment_image_tag @user, :profile_image, format: 'jpeg', size: "100x100", fallback: "no_image.jpg" %>
	   <%= @user.name %></td>
     			<td><%= @book.title %></td>
     			<td><%= @book.body %></td>
     			<td class="btn"><%= link_to "Edit",edit_book_path(@book.id) %></td>
     			<td class= "btn"><%= link_to "Destroy",book_path(@book.id), method: :deleate,"data-confirm" => "本当に消しますか？"  %></td>
     		</tr>
     	</tbody>
     </table>

</div>